# Address Book API

FastAPI-based Address Book application using SQLite.

## Run Instructions

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload
```

Visit Swagger UI:
http://127.0.0.1:8000/docs
